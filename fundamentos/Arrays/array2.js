
const palavras = []; 

palavras.push("domingo");
palavras.unshift("estudar");
palavras.pop();
palavras.shift();


// imprimi o array só usando variável (sem iterar manualmente)
console.log(palavras);